name: budian_mobile
description: Bu Dian Allergen Detection Mobile App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.0
  http: ^1.2.0
  image_picker: ^1.0.0
  lucide_icons: ^0.257.0
  shared_preferences: ^2.2.0
  cached_network_image: ^3.3.0
  flutter_svg: ^2.0.0
  intl: ^0.19.0
  percent_indicator: ^4.2.0
  connectivity_plus: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true